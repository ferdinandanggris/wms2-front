import React from 'react';


const Home = () => {

    return (
        <div>
            <h1>Warehouse</h1>
        </div>
    );
};


export default Home;
// export default connect(mapStateToProps, { getSync, syncNow })(Home);

